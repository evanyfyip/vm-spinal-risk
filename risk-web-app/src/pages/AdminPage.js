import React from 'react';

function AdminPage() {
  return <h1>Features: Import new training data and retrain model, export existing training data, show metrics on training data.</h1>;
}

export default AdminPage;